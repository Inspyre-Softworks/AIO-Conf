{
  "options": [
    {
      "name": "debug",
      "type": "bool",
      "default": false,
      "env": "DEBUG",
      "cli": ["--debug"],
      "description": "Enable debug logging."
    },
    {
      "name": "database",
      "type": "dict",
      "default": {
        "host": "localhost",
        "port": 3306
      },
      "env": "DB",
      "description": "Database settings; nested environment keys use DB__HOST and DB__PORT."
    }
  ]
}
